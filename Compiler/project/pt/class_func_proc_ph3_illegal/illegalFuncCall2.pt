program primes (output) {
    function test2 (var f: integer):boolean {
  	    f =2;
	    return (true);
    }

    var i: integer;
    var j: boolean;

    i = test2(i); //ERROR: type mismatch in assignment
}
